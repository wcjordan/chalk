apiVersion: v1
kind: Service
metadata:
  name: {{ include "..fullname" . }}-ui
  labels:
    {{- include "..uiLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 19006
      targetPort: 19006
      protocol: TCP
      name: http
  selector:
    {{- include "..uiSelectorLabels" . | nindent 4 }}
